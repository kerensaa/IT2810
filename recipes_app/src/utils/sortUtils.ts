import { useState } from 'react';
import { RecipeType } from '../types';

export function useSorting(_event: unknown, data: RecipeType[]) {
  const handleSorting = () => {};

  return { handleSorting };
}
